spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/energy_db
    username: postgres
    password: luzenith9424
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080
  error:
    include-message: always